const userRouter = require("./usersRoutes");
const shopListRouter = require("./shopListRoutes");
const shopRouter = require("./shopRoutes");

module.exports = {
  userRouter: userRouter,
  shopListRouter: shopListRouter,
  shopRouter: shopRouter,
};
